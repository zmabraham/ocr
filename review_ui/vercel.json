{
  "buildCommand": "cd review_ui && npm install && npm run build",
  "outputDirectory": "review_ui/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://ocr-production-e0be.up.railway.app/api/:path*"
    }
  ]
}
