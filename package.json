{
  "name": "hebrew-ocr-workflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd review_ui && npm run build",
    "postinstall": "cd review_ui && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
